MEMORY {
    ZP_CHRGET: start = $0000, size = $0018, fill = yes, file = %O;
    DUMMY:	   start = $0000, size = $0100, file = "";
}
SEGMENTS {
    CHRGET:    load = ZP_CHRGET, type = ro;
    ZPCHRGET:  load = DUMMY;
}
